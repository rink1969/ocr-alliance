# OCR 联合工作组

我们有大量通过扫描纸质文件得到的图片，包含复杂的排版，盖章，手写文字等，需要OCR识别成文本。

现有的OCR工具识别成功率虽然已经很高，但是还是达不到100%的正确率。

因此我们设想并行通过多个OCR工具去识别，然后再由一个大语言模型把多个识别结果统合起来，达到更高的识别成功率。

# 应用
整个应用是一个带WebUI的桌面应用。

## 模型
应用内置三个目前最好的OCR模型：
1. PaddleOCR-VL-1.6
2. HunyuanOCR
3. GLM-OCR

让用户填入一个统合结果的大语言模型，包括baseURL，modle id等信息。

## 处理模式
因为要处理大量的文件，处理过程为批处理模式。

输入是用户选择的一个文件夹，输出为用户选择的另外一个文件夹。

应用递归处理处理输入文件夹，ocr识别的内容按同样的目录结构和文件名放在输出文件夹。

每个输入的图片，对应四个ocr结果文件，分别用他们的模型名字做后缀。

## 保持进度

因为是大批量的识别，可能中间会因为各种情况而中断。

应用要记录进度，重启之后可以继续处理。

## 结果展示

左边展示输入文件夹的目录树，用户点击选择某个图片之后，在右侧内容区分别展示原始图片，三个OCR模型识别结果，以及大语言模型统合之后的结果。并将三个OCR模型识别结果中不一致的地方高亮显示出来。

## release

需要 win+x86_64/mac+arm/linux+x86_64 三个平台的二进制包

模型可以只建好目录，让应用自己下载，但是需要告诉用户去哪里下载，怎么放到对应的目录里。
